apiVersion: v1
kind: Service
metadata:
    name: nodeport-service
spec:
    type: NodePort
    selector:
      app: ip-app
    ports:
    - nodePort: 30000
      port: 80
      targetPort: 80